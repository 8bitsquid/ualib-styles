module.exports.register = function(handlebars) {
    handlebars.registerHelper('ifRoot', function(options) {
        return options.fn(this);
        /*if (!this.rootNumber) {
            return '';
        }
        return (root == this.rootNumber) ? options.fn(this) : options.inverse(this);*/
    });
}